# Progress
> Hot tier. Live handoff + terse done log. Outcome summaries, NOT a copy of tasks.md checkmarks.
> Per-task detail lives in the commit + the approved plan (`docs/plans/`) + ADRs (`decisions.md`).

## Handoff (current) — project bootstrapped; awaiting v1 scope decision
- 2026-06-15: **Project created + memory scaffolded.** New Eclipse Theia project (brand: Synlinea), sibling to `cli-ide`. Founding doc `docs/concept.md` (cross-CLI skill/subagent integration tool). Memory tiers + Lean `CLAUDE.md` + ADR-0001 (Theia base) in place. **No code yet.**
- **NEXT**: user decides `concept.md` §10 — Q1 (first cut: compiler / management-UI / launcher), Q2 (Codex projection: AGENTS.md-embed / MCP-tool / accept-degrade), v1 scope — then write the first Phase-1 plan in `docs/plans/`. Optional precursor: a minimal Theia spike to size the work.

## Done log (newest first — one line; detail in commit / plan / ADR)
- **Bootstrap** — memory scaffold (docs/ tiers, CLAUDE.md Lean, ADR-0001 Theia, glossary seeded from concept.md). 2026-06-15.

## History (resolved, terse)
- Predecessor `cli-ide` (Vue Synlinea, T1–T10) demoted to reference / domain-logic source per ADR-0001.
